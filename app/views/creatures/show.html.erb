<h2><%= @creature.name %></h2>
<p>
  <%= @creature.description %>
</p>

<%= link_to 'Back to Universe', universe_creatures_path(@universe) %>
<%= link_to 'Edit', edit_creature_path(@creature) %> | 
<%= link_to 'Delete', @creature, method: :delete, data: { confirm: 'For real?' } %>